# This file defines a character. So, instead of typing the same five lines over and over with diffent variables, you can just make a Character object and keep everything contained and neat.

class Character :
    # Idk what the variables really mean right now.